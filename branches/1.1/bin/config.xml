<?xml version="1.0" encoding="windows-1251"?>
<cfg>

<!-- 
    Path to log file.
-->
<log_file>C:\ioctls.txt</log_file>

<!--
    If true, hex dumps will be logged.
-->
<hex_dump>false</hex_dump>

<!--
    If true, will print logging output to console.
-->
<log_requests>true</log_requests>

<!--
    If true, will print logging output to kernel debugger.
-->
<debug_log_requests>true</debug_log_requests>

<!--
    If true, will fuzz IRPs.
-->
<fuze_requests>false</fuze_requests>

<!--
    If true, will also randomly modify fuzzed IRP's input buffer size.
-->
<fuze_size>false</fuze_size>


<!--
    IRP "allow" list.

    The fuzzer will process (i.e. log and/or fuzz) any IRP request 
    containing at least one parameter from the <allow> list.

    If the list is empty, each IRP will be processed.
-->
<allow>
    <!--
	    IRP destination driver name
    -->
    <drivers>
        <entry>kl1.sys</entry>
        <entry>dwall.sys</entry>
        <entry>fsdfw.sys</entry>
    </drivers>
    
    <!--
	    IRP destination device name
    -->
    <devices>
        <entry>\Device\kimul9</entry>
        <entry>\Device\dwall</entry>
        <entry>\Device\FSDFW</entry>
    </devices>
    
    <!--
	    IRP I/O Control Code
    -->
    <ioctls>
        <entry>0x0022c008</entry>
        <entry>0x0022208c</entry>
        <entry>0x0007e08c</entry>
    </ioctls>
    
    <!--
	    IRP sender process file path/name
    -->
    <processes>
        <entry>avp.exe</entry>
        <entry>DefenseWall.exe</entry>
        <entry>fsdfwd.exe</entry>
    </processes>
    
</allow>


<!--
    IRP "deny" list.
    Can be empty.
-->
<deny>

<!-- "deny" list is identical in structure to "allow" list -->

</deny>

</cfg>
